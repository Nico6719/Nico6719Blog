// .vitepress/config.mts
import { defineConfig } from 'vitepress'
import { AnnouncementPlugin } from 'vitepress-plugin-announcement'

// ============================================================
// VitePress 配置文件
// 参考: https://vitepress.yiov.top/
// GitHub: https://github.com/Yiov/vitepress-doc
// ============================================================

export default defineConfig({
  // ============================================================
  // 站点基础配置
  // ============================================================
  
  // 网站标题 - 显示在浏览器标签和导航栏
  title: "NicoNotFind",
  
  // 网站描述 - 用于 SEO 优化
  description: "一个不知名的小网站 - 分享技术教程与个人项目",
  
  // 网站语言
  lang: 'zh-CN',
  
  // 站点图标（需要将 favicon.ico 放在 public 文件夹）
  head: [
    ['link', { rel: 'icon', href: '/favicon.ico' }],
    // SEO 优化相关 meta 标签
    ['meta', { name: 'author', content: 'Nico6719' }],
    ['meta', { name: 'keywords', content: 'VitePress, 博客, 技术文章, 我的世界, 服务器教程' }],
    // 开启移动端优化
    ['meta', { name: 'viewport', content: 'width=device-width, initial-scale=1.0' }]
  ],
  
  // 开启纯净链接（去掉 .html 后缀）
  // 例如: /about.html → /about
  cleanUrls: true,
  
  // 最后更新时间戳
  lastUpdated: true,
  
  // Markdown 配置
  markdown: {
    // 显示行号
    lineNumbers: true,
    // 图片懒加载
    image: {
      lazyLoading: true
    }
  },

  // ============================================================
  // Vite 插件配置
  // ============================================================
  
  vite: {
    plugins: [
      // 公告栏插件配置
      AnnouncementPlugin({
        // 公告标题
        title: '🎉 网站公告',
        
        // 公告内容 - 使用数组可以添加多条内容
        body: [
          {
            type: 'text',
            content: '📢 欢迎访问 NicoNotFind 个人博客！',
            style: 'font-size: 16px; font-weight: bold; margin-bottom: 10px;'
          },
          {
            type: 'text',
            content: '💡 本站持续更新技术教程和个人项目，欢迎收藏关注！',
            style: 'font-size: 14px; color: #666; line-height: 1.6;'
          },
          {
            type: 'text',
            content: '🚀 最新更新：我的世界服务器搭建教程已上线',
            style: 'font-size: 14px; color: #1890ff; margin-top: 8px;'
          }
        ],
        
        // 底部按钮区域（可选）
        footer: [
          {
            type: 'text',
            content: '感谢您的访问 ❤️',
            style: 'margin-right: 10px;'
          },
          {
            type: 'button',
            content: '查看详情',
            link: '/abme',
            props: {
              type: 'primary' // 按钮类型: primary | success | warning | danger
            }
          }
        ],
        
        // 显示时长配置
        // 0 = 不自动关闭
        // 5000 = 5秒后自动关闭
        // -1 = 只显示一次（关闭后不再显示）
        duration: 0,
        
        // 是否允许重新打开公告
        reopen: true,
        
        // 移动端是否自动最小化
        mobileMinify: true,
        
        // 是否启用闪烁提示效果
        twinkle: false,
      })
    ]
  },

  // ============================================================
  // 主题配置
  // ============================================================
  
  themeConfig: {
    // 网站 Logo（需要将图片放在 public 文件夹）
   // logo: '/logo.png',
    
    // 站点标题（可选，不设置则使用 title）
    siteTitle: 'NicoNotFind',
    
    // ============================================================
    // 导航栏配置
    // ============================================================
    nav: [
      { 
        text: '🏠 主页', 
        link: '/' 
      },
      { 
        text: '👤 关于', 
        link: '/abme' 
      },
      // 下拉菜单示例
      {
        text: '📚 教程',
        items: [
          { text: '服务器教程', link: '/howtosetupnewmcservers' },
          // 可以添加更多教程链接
        ]
      }
    ],
    
    // ============================================================
    // 侧边栏配置
    // ============================================================
    sidebar: [
      {
        text: '📖 关于',
        // collapsed: false, // 是否默认折叠
        items: [
          { 
            text: '个人介绍', 
            link: '/abme' 
          },
          { 
            text: '个人项目', 
            link: '/abmezp' 
          },
          { 
            text: '友情链接', 
            link: '/link' 
          },
        ]
      },
      {
        text: '🎮 服务器相关教程',
        items: [
          { 
            text: '如何搭建一个我的世界服务器', 
            link: '/howtosetupnewmcservers' 
          }
        ]
      }
    ],
    
    // ============================================================
    // 页脚配置
    // ============================================================
    footer: {
      message: '闽ICP<a href="https://beian.miit.gov.cn/" target="_blank">备2025115232号-1</a>',
      copyright: 'Copyright © 2025-present <a href="https://github.com/Nico6719" target="_blank">Nico6719</a>'
    },
    
    // ============================================================
    // 社交链接
    // ============================================================
    socialLinks: [
      { 
        icon: 'github', 
        link: 'https://github.com/Nico6719' 
      }
      // 可以添加更多社交链接，支持的图标：
      // github, twitter, discord, slack, facebook, instagram, linkedin, youtube
    ],
    
    // ============================================================
    // 编辑链接配置
    // ============================================================
    editLink: {
      pattern: 'https://github.com/Nico6719/Nico6719Blog/edit/main/docs/:path',
      text: '✏️ 在 GitHub 上编辑此页面'
    },
    
    // ============================================================
    // 最后更新时间配置
    // ============================================================
    lastUpdated: {
      text: '📅 最后更新于',
      formatOptions: {
        dateStyle: 'short', // 日期样式: full | long | medium | short
        timeStyle: 'short'  // 时间样式: full | long | medium | short
      }
    },
    
    // ============================================================
    // 搜索配置 - 本地搜索
    // ============================================================
    search: {
      provider: 'local',
      options: {
        translations: {
          button: {
            buttonText: '搜索文档',
            buttonAriaLabel: '搜索文档'
          },
          modal: {
            noResultsText: '无法找到相关结果',
            resetButtonTitle: '清除查询条件',
            footer: {
              selectText: '选择',
              navigateText: '切换',
              closeText: '关闭'
            }
          }
        }
      }
    },
    
    // ============================================================
    // 文档页脚 - 上一页/下一页
    // ============================================================
    docFooter: {
      prev: '⬅️ 上一页',
      next: '下一页 ➡️'
    },
    
    // ============================================================
    // 右侧大纲配置
    // ============================================================
    outline: {
      level: [2, 3],  // 显示 h2 和 h3 标题
      label: '📑 页面导航'
    },
    
    // ============================================================
    // 移动端菜单按钮文字
    // ============================================================
    sidebarMenuLabel: '菜单',
    returnToTopLabel: '回到顶部',
    
    // ============================================================
    // 暗黑模式切换按钮文字
    // ============================================================
    darkModeSwitchLabel: '主题',
    lightModeSwitchTitle: '切换到浅色模式',
    darkModeSwitchTitle: '切换到深色模式',
    
    // ============================================================
    // 外部链接图标
    // ============================================================
    externalLinkIcon: true,
    
    // ============================================================
    // 404 页面配置
    // ============================================================
    notFound: {
      title: '页面未找到',
      quote: '看起来你访问的页面不存在',
      linkText: '返回首页',
      linkLabel: '返回首页'
    }
  }
})
